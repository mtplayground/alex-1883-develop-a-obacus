function App() {
  return (
    <main className="app-shell">
      <section className="intro" aria-labelledby="page-title">
        <p className="eyebrow">Schoty abacus</p>
        <h1 id="page-title">Vite React TypeScript project ready</h1>
        <p>
          This repository is initialized with a typed React entry point and
          working Vite development, preview, and production build scripts.
        </p>
      </section>
    </main>
  );
}

export default App;
